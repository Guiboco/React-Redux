const defaultState = 0;

function reducer(state = defaultState, { type, payload}){
    switch(type) {
        // case value:
            
        //     break;
    
        default:
            return state;
    }
}

export default reducer;